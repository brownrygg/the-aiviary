# ============================================================================
# Docker Compose Project Name (ensures isolation from other stacks)
# ============================================================================
COMPOSE_PROJECT_NAME=aiviary-tree

# ============================================================================
# Client VM Configuration
# ============================================================================

# Client Identity (unique per client)
CLIENT_ID=test-client-rikk
VM_API_KEY=generate_with_openssl_rand_hex_32

# Domain Configuration
DOMAIN=localhost
N8N_DOMAIN=n8n2.leftsideofhistory.cc

# Web UI Ports
N8N_WEB_UI_PORT=5678

# ============================================================================
# PostgreSQL Configuration
# ============================================================================
POSTGRES_USER=rikk
POSTGRES_PASSWORD=generate_with_openssl_rand_base64_32
POSTGRES_DB=n8n
POSTGRES_PORT=5432
POSTGRES_NON_ROOT_USER=postgres-non-root
POSTGRES_NON_ROOT_PASSWORD=generate_with_openssl_rand_hex_32

# ============================================================================
# Security & Encryption
# ============================================================================
# CRITICAL: Never change ENCRYPTION_KEY after first use!
ENCRYPTION_KEY=generate_with_openssl_rand_hex_32

# ============================================================================
# n8n Configuration
# ============================================================================
N8N_VERSION=1.122.3
TIMEZONE=Europe/Zagreb
N8N_TRUST_PROXY=0.0.0.0/0
WEBHOOK_URL=https://${N8N_DOMAIN}
VUE_APP_URL_BASE_API=https://${N8N_DOMAIN}

# ============================================================================
# Aiviary Chat UI Configuration (replaces OpenWebUI)
# ============================================================================
# JWT secret for authentication - MUST be secure in production
# Generate with: openssl rand -hex 32
JWT_SECRET_KEY=generate_with_openssl_rand_hex_32

# Environment (development or production)
ENVIRONMENT=production

# ============================================================================
# AI API Keys
# ============================================================================
# Google API Key for Gemini (legacy - kept for compatibility)
GOOGLE_API_KEY=your_google_api_key_here

# Google Cloud Configuration (Vertex AI for embeddings)
# Optional: Only needed if using Vertex AI instead of standard Gemini API
GOOGLE_CLOUD_PROJECT=your_project_id
GOOGLE_APPLICATION_CREDENTIALS=/app/credentials/service-account.json
VERTEX_AI_LOCATION=us-central1

# Anthropic API key for Claude (used by analytics agent)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# ============================================================================
# Redis Configuration
# ============================================================================
REDIS_VERSION=8.4.0-alpine

# ============================================================================
# Cloudflare Tunnel (optional)
# ============================================================================
CLOUDFLARE_TUNNEL_TOKEN=your_tunnel_token_here
CLIENT_ID_BROKER=test-client-rikk
